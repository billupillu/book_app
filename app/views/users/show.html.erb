
<section>
	<div class='col-sm-10 col-sm-offset-1'>
		<header>
			<h2 class='titles'><%= "#{@user.username}" %>'s Profile</h2>
		</header>
		<h3 id="email">Email: <%= @user.email %></h3>
	</div>
</section>
<%= link_to 'Destroy', @user, method: :delete, data: { confirm: 'Are you sure?' } %>